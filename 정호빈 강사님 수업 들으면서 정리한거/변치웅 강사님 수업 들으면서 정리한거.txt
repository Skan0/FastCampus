선택 셀 위에 셀 생성 - a
선택 셀 병합 shift + m
마크다운 편집 - m
에디트 편집 y
선택 셀 삭제 dd
#'''
#'''
-> 여러줄 주석처리

입력 저장 출력 처리
 
문자열 > 실수 > 정수
큰범주에서 작은 범주로는 들어갈 수 없다

round 반올림
round(a, 원하는 소숫점까지)
new format string 
f'{변수명:0자릿수 뒤에 소숫점과 합쳐서 부족한 자릿수는 0으로 채운다.소숫점 자릿수}'

조건 연산
멤버연산자
  '안녕' in '안녕하세요'

자료구조
데이터의 집합
  리스트
인덱싱은 하나의 값에 접근 슬라이싱은 여러개의 값의 묶음에 접근  
[시작인덱스 : 마지막 인덱스 + 1]
마지막 인덱스까지 읽고 싶은데 모르면 :뒤를 비워 두면 된다
0부터 시작하는것도 동일하다
2의배수만 가져와라는 Step
[첫번째:마지막:건너뛸 간격] [0:10:2]와 같이

내부명령어 (메소드)

튜플
set{ }

흐름제어문(controlflow)
  if, elif, else
  for, while
  break, try, except, continue
흐름제어문의 구성
  순차 : 위에서부터 아래 방향으로 코드가 순서대로 실행된다.
  분기 : 판별문
  반복 : while, for 혹은 함수, 클래스

for in range(x,y,z) 
 z값이 슬라이싱 할 때 띄엄 띄엄 가던 것 처럼 해준다

